---
interface Props {
  name: string;
}

const { name } = Astro.props;
---

<a
  href={`/tags#${name}`}
  class="rounded-xs border border-border px-2 py-1 font-mono text-xs no-underline hover:border-link-hover"
>
  {name}
</a>
